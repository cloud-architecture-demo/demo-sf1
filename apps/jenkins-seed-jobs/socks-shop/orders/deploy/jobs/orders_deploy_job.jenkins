#!/usr/bin/env groovy

pipelineJob('orders-deploy-jenkins-operator') {
    displayName('orders-deploy')

    logRotator {
        numToKeep(30)
        daysToKeep(90)
    }

    configure { project ->
        project / 'properties' / 'org.jenkinsci.plugins.workflow.job.properties.DurabilityHintJobProperty' {
            hint('PERFORMANCE_OPTIMIZED')
        }
    }

    definition {
        cpsScm {
            scm {
                git {
                    remote {
                        url('https://github.com/cloud-architecture-demo/demo-sf1.git')
                        credentials('jenkins-operator')
                    }
                    branches('*/dev')
                }
            }
            scriptPath('apps/jenkins-seed-jobs/socks-shop/orders/deploy/pipelines/orders_deploy_pipeline.jenkins')
        }
    }
}