---
apiVersion: v1
kind: Service
metadata:
  name: catalogue
  labels:
    name: catalogue
spec:
  selector:
    name: catalogue
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80

